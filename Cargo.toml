[package]
name = "nano-mesh-rs"
version = "0.2.0"
edition = "2021"
description = "Board-agnostic mesh networking layer wrapping embedded-nano-mesh"

[lib]
name = "nano_mesh"

[dependencies]
embedded-nano-mesh = "2.1"
embedded-io = "0.6"
